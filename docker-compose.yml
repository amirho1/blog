services:
  blog:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - blog-data:/app/data
    env_file:
      - .env

volumes:
  blog-data:
